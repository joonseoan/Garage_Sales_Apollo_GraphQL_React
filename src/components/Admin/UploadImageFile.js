import React from 'react';

export default () => {
    return (
        <div>
            <label htmlFor="imageFile" />
            <input type="file" name="imagePath" id="imageFile" />
        </div>
    );
}